# Forsaken Mail 二开计划（A 路线：小修小补，快速可用）

## 目标
把原始项目从“在线实时演示版”改到“可自用、可保留历史邮件、基础安全可接受”的状态。

## 第一阶段（先做）
1. 引入 SQLite 持久化邮件
2. 增加邮件列表/详情 API
3. 页面刷新后可查看历史邮件
4. 自定义邮箱前缀做基础校验
5. HTML 内容做基础净化/隔离展示
6. 增加 TTL 清理能力
7. 补环境配置与启动说明

## 第二阶段（后做）
1. 附件支持
2. 限流
3. 多域名
4. 管理后台
5. Docker Compose / 生产化部署

## 设计取舍
- 存储先用 sqlite，低门槛
- 接口继续用 Express，避免重构过大
- 实时推送保留 socket.io，历史数据改为 API 拉取
- HTML 先做 sanitize，后续可改 iframe sandbox / 更强白名单
